import 'package:flutter/material.dart';
import 'klasy/karta_wpisu.dart';
class FirstScreen extends StatelessWidget
{
  @override
  Widget build(BuildContext context) {
 return Container(
   child: Center(
     child: karta(

    ),
 ),
    );
  }
}